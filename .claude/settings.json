{
  "permissions": {
    "allow": [
      "Bash(pio device list *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "clang-format -i $CLAUDE_FILE_PATH 2>/dev/null; true"
          },
          {
            "type": "command",
            "command": "echo \"$CLAUDE_FILE_PATH\" | grep -qiE \"[/\\\\]data[/\\\\][^/\\\\]*\\.(js|css|html|json|svg|png|ico|woff2?)$\" && echo '{\"systemMessage\":\"data/ asset changed - run: pio run -t uploadfs  (UI changes do NOT reach the ESP32 until the LittleFS image is re-uploaded).\"}'; true"
          }
        ]
      }
    ]
  }
}
